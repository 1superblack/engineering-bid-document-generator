# ADR-007: 评分项聚合进连贯技术章节骨架（修复碎片化与跨章串味）

## Status
Accepted

## Context
实测「3片区」招标文件生成 280 页技术标时，产出 116 个 H1 章节，其中 94 个是招标书
原文碎片（`日 17时`、`1.`、`□不允许 ☓允许`、评标办法散文等），真实技术章节被挤到末尾，
且出现「主要实施方案」章节内混入「政采贷/招标代理费/串通投标」等无关评分项响应（跨章串味）。
实际硬页数仅约 119 页（分页符 118 个），远低于目标。

根因：
1. `planner.plan_chapters` 在 `parse_result` 含 `score_items` 时，采用「**逐条评分项 = 独立 H1 章节**」
   （优先级1 直接覆盖默认连贯章节骨架）。解析器 `_collect_scores` 把日期/勾选项/评标散文也抽成
   score_items，于是 116 个碎片各成一章。
2. 渲染器 `_relevant_score_items` 用「最长公共子串 ≥ 3 字」把 score_items 匹配到章节；
   碎片章标题泛化导致误匹配，跨章串味。
3. `bid_generator.generate_bid` 把 demo 知识库公司名「示例建设集团有限公司」作为投标人名落盘，
   占位符泄漏。

## Decision
1. **planner 聚合（核心修复）**：`plan_chapters` 新增 `aggregate_score_items=True`（默认开）。
   当 score_items 存在时，**不再逐条建章**，而是：
   - `_filter_valid_score_items` 过滤掉无数值分值/超短/纯日期/勾选项的噪声 score_items；
   - 始终以 `DEFAULT_CHAPTERS`/`SERVICE_CHAPTERS` 连贯骨架作为章节列表；
   - `_route_score_item_to_chapter` 按关键词把每个有效评分项路由到**唯一**骨架章，挂到该章 `score_items`；
   - 章节 `score_weight` 取归属项分值之和，`page_ratio` 据此分配页数；`source='评分项聚合'`。
   - 未路由项不建章（由 scoring_reinforce 附录统一闭环，不造成碎片化）。
   - 设 `aggregate_score_items=False` 可回退旧行为（可逆）。
2. **跨章串味自愈**：骨架章标题具体（如「施工总进度计划及保障措施」），LCS≥3 模糊匹配对
   政采贷/代理费/串通投标等资信类项不再误命中任何技术章，无需改 renderer（保留 `test_v7_11` 语义）。
3. **占位符守卫**：`generate_bid` 推导 `bidder_name` 时，若公司名以「示例/演示」开头（demo 占位），
   不落盘，渲染器回退中性「我司」。真实投标人信息由 `user_context` 注入。

## Consequences
- 更易：技术标结构变为 13（施工）/7（服务）个连贯专业章节，页数分配合理，可读可用。
- 更易：评分项响应精准挂到对应技术章，不再跨章串味；资信/财务类项由附录闭环覆盖。
- 更易：无真实企业信息时不再泄漏 demo 公司名。
- 更难：需更新 `test_planner_and_models.test_score_items_driven_planning`（其旧契约即本要修的 bug）。
- 更难（权衡）：过度细分的独特评分小类若不匹配任何骨架关键词，将不单独成章（仍计入附录闭环，
  不丢失覆盖），以换取结构连贯——这是有意的取舍。
