# ADR-009: 章节源自招标文件 + 格式硬化（编号/附表/黑色/匿名/空标题）

## Status
Accepted

## Context
用户审查 200 页三片区技术标成品后，指出六类问题：
1. 需求 200 页却生成 525 页（C22 专项补全尾巴 + 篇幅估算失真）；
2. 标题「15 应急救援预案」下被塞入「15.1~15.5 附表」（编号逻辑把附表吸进正文序列）；
3. 15.5 后约 400 页无次级标题的序号文本（C22 补全尾巴未挂标题直接灌入）；
4. 段落开头机械套话（去模板化用力过猛，非本轮重点）；
5. 字体颜色不全黑（源 PDF 彩色 run / 偏差表红绿被原样带入）；
6. 部分标题后无正文（空壳标题）。

经对照三份参考文件（技术标大体格式 / 黄琰·陈卓样例 / 三镇中心招标书），确认：
- 章节名称与数量**由当次招标文件评分项决定**，非固定 13 章模板（文件 A 仅为格式参考）；
- 一级中文「一、」/ 二级「（一）」/ 三级「1．」；附表独立区块 2.1~2.6+材料，不占正文编号；
- 本项目（三镇中心）暗标=否，但用户明确要求「去投标人名」→ 统一用「（投标人名称）」占位。

## Decision
1. **彻底关闭 C22 专项补全**：`entry.py` 中 `enable_special_scheme` 默认值由 `True` 改为 `False`，C22 不再进入管线（保留类与函数，可逆）。
2. **重写 `DocFormatStage` 编号逻辑**（原 C24）：
   - H1 → 中文「一、二、…、十三、」；H2 → 「（一）（二）…」；H3 → 「1．2．3．」；
   - 遇到「附表」H1 起，其下所有标题（含 2.1~2.6 子表）**跳过编号、保留原样**，杜绝 15.1~15.5；
   - 编号前新增**空标题清理**：删除其后无正文/表格的孤立标题（附表块整体排除）。
3. **全局置黑**：在 `DocxSanitizeStage` 末尾对所有 paragraph/table run 强制 `font.color=RGBColor(0,0,0)`，消除彩色残留。
4. **去投标人名统一占位**：`page_elements.py` 封面公司名回退值由「（投标单位）」改为「（投标人名称）」；runner 不注入真实公司名，与 C27 法律文书模板占位一致。
5. **页数动态参数**：`target_pages` 由 runner 按需求传入（非硬编码）；C22 关闭后主体仅为招标评分章，篇幅收口。

## Consequences
- 正面：章节严格等于招标文件评分所得；编号符合中文技术标体例；附表独立且不被误编号；全文黑色；匿名占位统一；无 C22 冗余尾巴与空标题。
- 负面/风险：`target_pages` 与 Word 实际页数约 2.6× 估算失真，需 runner 侧按经验系数折算（如目标 ~200 页设 `target_pages≈80`）；中文编号对超过 20 章的场景（极少见）需扩展 `_cn`；空标题清理若误删需回退（已对附表块豁免）。
- 可逆性：C22 仅需把 `enable_special_scheme` 改回 `True` 即恢复；其余均为规则化、可关。
