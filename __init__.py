# v8.0: 此文件原为 bid_technical/tables/__init__.py 的内容，
# 扁平化打包时误置于项目根目录。根目录不应是 Python 包，
# 清空以避免 pytest 加载 conftest 时触发 gantt→docx 链式导入。
# gantt 的 generate_gantt_table 可通过 from gantt import generate_gantt_table 直接访问。

