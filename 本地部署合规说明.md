# 工程标书生成器 · 本地部署合规说明

> 适用场景：央企 / 国企 / 军工涉密单位的招投标技术标编制，需满足**数据不出本机**、**等保/国密/涉密对标**的选型要求。
> 文档性质：选型与合规沟通材料，不含代码改动；所有能力均以本地运行为前提。

---

## 1. 核心结论（一句话）

**标书生成全链路在用户本机完成，原始招标文件、企业资质业绩、生成稿均不离开本地；无强制联网、无强制云依赖、无后台上报。**

---

## 2. 数据物理隔离

| 数据类别 | 生命周期 | 是否出本机 | 说明 |
|---|---|---|---|
| 招标文件（docx / PDF） | 解析→生成→产出 | 否 | 仅在本地进程内读取，不落第三方服务 |
| 企业资质 / 业绩 / 人员库 | 用户本地 `user_knowledge_base.json` | 否 | 纯本地文件，随工程复制，不上云 |
| 生成稿（docx / md / 图表） | 输出到用户指定目录 | 否 | 用户完全掌控落盘位置 |
| LLM 扩写（可选） | 仅当配置本地模型/自托管端点时 | 否* | 默认关闭；云端仅作**可选**通道，且非必选 |
| LLM 扩写 · 云端通道 | 仅当显式选择「云端 API」后端且配置密钥时 | 出网＋脱敏 | 出网前对投标单位名/人名/证书号/业绩名做**可逆占位脱敏**，云端不接触真实实体；回传后本地还原 |

> *若企业选择接入合规自托管大模型（如内网部署的 OpenAI 兼容服务），通信范围仅限该内网端点，不经过公网第三方。
> 云端通道的脱敏由 `llm_mask.Masker` 在 `LLMClient` 出网边界自动完成，属「用云端提质、数据不出本机」的折中方案，非默认路径。

---

## 3. 本地优先 / 零强制联网

- **解析与生成主路径均可离线运行**：正则解析 + 模板/规则引擎生成不依赖任何外部服务。
- **LLM 为可选增强**：`enable_llm_core`、`enable_llm_parse` 默认关闭；断网时自动降级为正则/模板，保证可出稿。
- **新图形/检索依赖均为可选**：matplotlib（真·甘特图）、OCR（图片查重）、本地 embedding（知识库语义检索）缺失时**优雅回退**，不阻断主流程。
- **无遥测 / 无埋点**：代码层不向任何外部地址上报使用数据、文件内容或统计信息。

---

## 4. 零幻觉与内容可信

- 生成层内置**禁止编造资质/业绩/人名/编号**硬约束（见 `tech_opt.build_generation_constraints` 与 `knowledge_base`）。
- 知识库 RAG 为**可选资产**，素材必须真实；事实核查（`FactCheckStage`，`enable_kb_rag` 默认关闭）对生成稿做「知识库未收录即告警」，回流需人工确认，杜绝自动污染。
- 企业画像能力已按 ADR-005 删除，不默认加载任何企业私密数据；用户资料完全由本人维护。

---

## 5. 等保 / 国密 / 涉密对标要点

> 以下为「对照检查清单」式叙述，便于合规评审逐项核对；具体等保/密评以权威测评机构结论为准。

| 对标维度 | 本工具支撑点 | 单位需自证项 |
|---|---|---|
| **等保 2.0（三级）· 身份鉴别** | 本地文件权限由 OS 管控；可选 License 鉴权 | 终端准入、域控、双因子 |
| **等保 · 数据保密性** | 数据不出本机；无外部传输 | 磁盘加密（BitLocker / 国密 SM4 磁盘加密） |
| **国密算法（SM2/SM3/SM4）** | 文档产物为标准 OOXML，可在交付侧套密 | 签章/加密环节接入合规国密中间件 |
| **涉密载体管控** | 生成稿落盘位置由用户指定，便于纳入涉密载体台账 | 涉密计算机、红黑隔离、打印刻录审计 |
| **供应链安全** | 依赖均为 Python 标准库 + 少量可审计开源库；无闭源黑盒 | 开源组件 SBOM 与漏洞扫描 |

---

## 6. 适用与不适用边界

**适用**
- 单机 / 内网环境的投标技术标编制，对数据驻留有硬要求。
- 央企、军工、科研院所等涉密或敏感行业的可控终端。

**不适用（须另行评估）**
- 需要多人协同、云端版本管理、集中审签的团队场景（与本地单用户定位冲突）。
- 商务标 / 造价 / 工程量清单联动（本期未纳入技术标专项）。

---

## 7. 部署与运维建议（最小合规基线）

1. 在**涉密/内网终端**安装运行，禁用公网出口或仅放行自托管模型端点。
2. 启用 OS 级磁盘加密与文件权限管控。
3. 企业资质业绩库（`user_knowledge_base.json`）按涉密载体管理，定期人工核对真实性。
4. 交付前使用内置「暗标零泄漏硬化」「三维查重」「零幻觉核查」做最终自检。
5. 保留生成日志与产出文件，满足审计可追溯要求。

---

## 8. 外置 API 双选项与出网脱敏（T2 · 用户可选）

> 工程设计上，「数据不出本机」是默认与红线；但用户可在**确认无强隐私风险的技术标**场景下，主动选择「云端 API」后端以追平 SaaS 正文质量。该选择附加强约束，非默认。

**两选项（本地优先）**
- **本地（默认，`backend="local"`）**：`base_url` 指向本机 vLLM/Ollama（如 `http://localhost:8000/v1`），数据全程不出本机，不做脱敏（保留个性化质量）。
- **云端（`backend="cloud"`）**：`base_url` 指向任意 OpenAI 兼容网关（DeepSeek / 通义 / 自建等），**出网前自动实体脱敏**（`mask=True`）。

**出网脱敏机制（`llm_mask.Masker`）**
- 脱敏实体来源：`req.user_context`（投标单位名、法定代表人、项目经理及关键人员姓名、执业证书号、类似业绩名、企业资质名）+ 本地知识库真实实体。
- 出网前将真实实体替换为可逆占位符（如 `[[ENT_0]]`），云端仅见占位符、不见真实实体；回传结果在本地 `unmask` 还原。
- 占位符不含任何真实信息，且无真实实体注入时脱敏为空操作（零成本）。

**隐私分级（商务标硬阻断）**
- 商务标（`bid_section="commercial"`）或非常规类型即便选了云端，也**拒绝出网**并回退本地模板生成，避免报价/造价等强隐私字段外泄。
- 技术标（主攻场景）无报价字段，按用户决策「外置API + 脱敏 + 两选项」开放云端通道。

**偏好持久化（`write_llm_config`）**
- 用户选择通过 `data/llm_config.json` 的 `backend` 字段持久化，默认 `local`（本地优先）；切换为 `cloud` 时 `mask` 默认开启。

---

*本说明随技术标优化清单（T2 / T10）一并维护，零代码风险；若合规口径更新，以最新版为准。*
