# ADR-008: 辅助产物集中归集到「_交付物」子文件夹

## Status
Accepted

## Context
用户实测反馈：把标书生成到桌面（`output_path` 指向 `C:\Users\pc\Desktop`）后，
桌面被约 15–20 个辅助文件淹没——废标风险自检报告、评分项命中矩阵、一致性校验、
三维查重、横道图、各 `.svg` 图纸、`_交付清单.md`、DEAI 副本 `_deai.docx`、
`评审报告.md` 等，全部以「`<主文档名>_XXX`」形式堆在主文档同目录。

根因：生成管线里**每个 Stage 各自 ad-hoc** 地把辅助产物写到
`result_path.parent`（即用户指定的主文档目录），缺少统一的「辅助产物输出目录」策略。
属于横切关注点散落的典型设计债。

用户的核心诉求：「我只需要你把标书的文档」——桌面只应出现那一个主 docx。

## Decision
引入集中路径策略模块 `pipeline/output_paths.py`，单一可控、易回滚：

- **主 docx 仍写到用户指定的 `result_path`**（桌面只留这一个文件）；
- **其余所有辅助产物统一归集到同名子文件夹** `<主文档名>_交付物/`，
  与主文档同父目录（如 `3片区工程技术标书_280页_交付物/`）；
- 新增可选硬开关 `emit_auxiliary=False`：完全不落盘辅助文件（仍保留在程序返回
  结果字典里，便于程序消费，只是不写文件）；
- 全部辅助写盘统一调用 `aux_path(ctx, result_path, suffix)` / `aux_dir(result_path, ctx)`，
  不再出现 `out.with_name(out.stem + ...)` 直落主文档目录的散落写法。

落地范围（全部改为走 `aux_path`/`aux_dir`）：
- `pipeline/report_stages.py`：所有 `_write_markdown`（废标/评分矩阵/一致性/专项方案/
  外壳套件/体检总览/文档格式/资格自查/需求闭环/国标格式等）；
- `pipeline/delivery_stage.py`：PDF 导出目录 + `_交付清单.md` 清单；
- `pipeline/schedule_stage.py`：`_施工进度横道图.md`；
- `pipeline/dedup3d_stage.py`：`_三维查重.md`；
- `pipeline/kb_stage.py`：`_零幻觉核查.md`；
- `pipeline/chart_stage.py`：甘特图 PNG / 各 SVG 图纸 / `_工程图表索引.md`；
- `generator.py`：`_评审报告.md`（旧引擎评审报告）；
- `deai/pipeline.py`：`_deai.docx`（去 AI 化副本）。

不在范围内：`repair/engine.py` 的 `_repaired.docx` / `_formatted.docx` ——
那是用户显式触发的「修复」工具，修复副本紧邻原文件是合理约定，不属于生成路径的噪音。

## Consequences
- **桌面干净**：生成后桌面仅见主 docx + 同名 `_交付物` 子文件夹；
- **审计线索不丢**：废标风险、评分命中、三维查重、图表等仍完整保留在子文件夹，
  投标前可随时翻查；
- **新增 Stage 约束**：后续写盘必须走 `aux_path`/`aux_dir`，否则会再次污染主目录
  （已在模块 docstring 标注）；
- **可逆性**：默认行为（子文件夹）与硬开关（`emit_auxiliary=False`）均为局部开关，
  回退到「全部直落主目录」只需移除本模块调用，无破坏性；
- **代价**：辅助文件多一层目录嵌套；`emit_auxiliary=False` 时本地无报告存档
  （仍可在返回值中取用）。
